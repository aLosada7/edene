import { Meta } from '@storybook/addon-docs';
import { css } from '@emotion/react';

import { colorsPalette, grays } from '@edene/foundations';
import { Group, Tag, Button, Stack, Text, Heading } from '@edene/components';

<Meta title="Tokens/Colors" />

# Colors

<p>The default button uses color as background, and hover when hovering</p>
<p>
    Tag uses light color as background color and color as color. Other
    components with a similar behaviour are: Alert
</p>

<Stack>
    <Group>
        <div
            style={{
                width: '100px',
            }}
        >
            Light
        </div>
        <div
            style={{
                width: '100px',
            }}
        >
            Color
        </div>
        <div
            style={{
                width: '100px',
            }}
        >
            Hover
        </div>
        <div
            style={{
                width: '100px',
            }}
        >
            Example Button
        </div>
        <div>Example Tag</div>
    </Group>
    {/* TODO: Do the same with all colors from palette when defined*/}
    <Group>
        <div
            style={{
                width: '100px',
                height: '100px',
                backgroundColor: colorsPalette.success.light,
            }}
        ></div>
        <div
            style={{
                width: '100px',
                height: '100px',
                backgroundColor: colorsPalette.success.color,
            }}
        ></div>
        <div
            style={{
                width: '100px',
                height: '100px',
                backgroundColor: colorsPalette.success.hover,
            }}
        ></div>
        <Button
            color="success"
            css={css`
                height: fit-content;
                margin: auto 0;
            `}
        >
            Button
        </Button>
        <Tag
            type="success"
            css={css`
                height: fit-content;
                margin: auto 0;
            `}
        >
            Tag
        </Tag>
    </Group>
</Stack>

<br />

## Grays

<Stack>
    {Object.keys(grays).map((key) => (
        <Group key={key}>
            {key}
            <div
                style={{
                    border: `1px solid ${grays[0]}`,
                    width: '20px',
                    height: 'inherit',
                    backgroundColor: grays[key],
                }}
            />
            <Text color={grays[key]}>Text</Text>
        </Group>
    ))}
</Stack>
