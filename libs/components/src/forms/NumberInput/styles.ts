import { css } from '@emotion/react';

export const numberInputWrapper = () => css`
    display: flex;
    align-items: center;
    justify-content: center;
    height: 44px;
`;

export const operationButton = css`
    border-radius: 50%;
`;

export const numberInputValue = css`
    box-sizing: border-box;
    min-width: 60px;
    text-align: center;
`;
