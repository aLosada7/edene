import { Header } from '../../shared/Header';
import { FlightTimeline as FlightTimelinePage } from '@edene/pro-labs';

const FlightTimeline = () => (
    <>
        <Header title="Flight Timeline" />
        <FlightTimelinePage />
    </>
);

export default FlightTimeline;
